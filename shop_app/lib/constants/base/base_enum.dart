

enum CustomDialogType {
  info,
  warning,
  error,
  success,
  question,
  noHeader,
}

enum ButtonType {
  primaryButton,
  secondaryButton,
}

enum StorageType {
  local,
  firebaseStorage,
  other,
}