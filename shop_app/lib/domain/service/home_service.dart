import 'package:flutter/material.dart';

class HomeService {
  static Future<void> prepareResource(BuildContext context) async {
    // await Future.delayed(const Duration(milliseconds: 1000));

  }
}
